# Example environment file (do NOT commit real secrets).
# Copy this to `.env` locally and fill values. `.env` is already ignored by `.gitignore`.

# --------------------
# Client-side (Vite) - prefix with VITE_ to expose to the browser
# These are safe to expose *only if* you understand they are public values.
# Giscus configuration (for comments widget)
VITE_GISCUS_REPO=owner/repo                # e.g. my-org/my-repo
VITE_GISCUS_REPO_ID=your_repo_id            # optional: giscus repo id
VITE_GISCUS_CATEGORY=General                # discussion category name
VITE_GISCUS_CATEGORY_ID=your_category_id    # optional: giscus category id

# EmailJS client keys (public key / service ids) - these are used client-side
VITE_EMAILJS_SERVICE=your_emailjs_service_id
VITE_EMAILJS_TEMPLATE=your_emailjs_template_id
VITE_EMAILJS_USER=your_emailjs_user_id

# --------------------
# Server-side (DO NOT expose in client; no VITE_ prefix)
# These should be set in your Vercel/Netlify/hosted env vars for serverless functions
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_OWNER=your_github_owner_or_org
GITHUB_REPO=your_github_repo_name

# Notes:
# - Never commit your real GITHUB_TOKEN. Keep it in your hosting provider's secret envs.
# - Vite only exposes variables that begin with VITE_ to client-side code. Do not prefix secrets with VITE_.