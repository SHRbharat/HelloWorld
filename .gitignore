# Node and package manager
node_modules/
.pnpm-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json   # optionally ignore if you prefer yarn or pnpm lock; remove if you want to commit

# Vite / build output
dist/
.build/
.vite/
out/

# Environment files (do NOT commit secrets)
.env
.env.local
.env.*.local
.local

# IDE / editor
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# OS files
.DS_Store
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
pnpm-debug.log*

# Test coverage
coverage/
.nyc_output/

# Temporary / cache
.cache/
.cache-loader/
parcel-cache/
.next/            # ignore if you ever use Next.js build output
.cache-vite/
.history/

# Storybook
storybook-static/

# Mac-specific
.AppleDouble
.LSOverride

# Misc
*.pem
*.key
*.crt

# Static site / deployment artifacts
gh-pages/         # if you use gh-pages branch local artifacts
dist/             # build output (already included above)

# Generated assets and local-only files
public/blogs/*.json     # if you generate local index files for blogs and don't want to commit them
public/.cache/

# Optional: lockfiles for other package managers (uncomment if you use them and want to ignore)
# pnpm-lock.yaml

.vercel
